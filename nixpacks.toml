[phases.setup]
nixPkgs = ["nodejs_20", "python3", "python3Packages.pip", "ffmpeg"]

[phases.install]
cmds = ["npm ci", "pip3 install -r requirements.txt --break-system-packages"]

[start]
cmd = "node server.js"
