# Viralityy — Python dependencies
# Updated: Tier 1 + Tier 2 agents integrated

# ── Gemini TTS (replaces ElevenLabs — uses existing Google AI paid plan)
google-generativeai>=0.8.0

# ── Core (unchanged) ────────────────────────────────────────────────────────
pymongo==4.6.1
motor==3.3.2
python-dotenv==1.0.0
requests==2.31.0
google-api-python-client==2.108.0
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1

# ── OpenAI — upgraded for agent support ─────────────────────────────────────
openai==1.52.0

# ── Agent 5: Trend Scraper — Google Trends (no API key needed) ─────────────
pytrends==4.9.2

# ── All other agents reuse packages already listed above ────────────────────
# Agent 1  Content Planner      → openai, motor
# Agent 2  Daily Topic Scout    → google-api-python-client, openai, motor
# Agent 3  Competitor Watcher   → google-api-python-client, openai, motor
# Agent 4  Script Research      → openai, requests, motor
# Agent 5  Trend Scraper        → pytrends, requests, openai, motor
# Agent 6  Auto-Voiceover       → requests, motor (ElevenLabs via requests)
# Agent 8  Competitor Scraper   → google-api-python-client, openai, requests, motor
